# 受控自动进化

## 结论

可以自动进化，但“发现问题”和“改变生产行为”必须分离。模型可以自动提出候选规则，不能直接在线改解析器、Cookie、代理、限流或任务状态机。

## 已落地的第一层

- 每个结果记录 `parser_version`、核心字段覆盖率、缺失字段和 HTML 哈希。
- 每个项目记录结构化失败码、尝试次数、租约恢复和完整状态事件。
- `/api/v1/metrics` 与界面汇总失败分布、解析版本和平均字段覆盖率。
- HTML 原文默认不落盘；开启证据后可构建固定回放集。

## 演进闭环

1. **Observe**：按站点、解析版本、执行模式统计成功率、阻断率和字段覆盖。
2. **Propose**：Agent 只能生成候选 Parser/Selector 或调度配置及原因，绑定 `candidate_version` 和证据集版本。
3. **Replay**：候选在离线 HTML 证据集运行，与当前生产版本对比字段正确率和回归。
4. **Gate**：必须满足覆盖率不下降、关键字段回归为零、异常率阈值和安全检查。
5. **Approve**：人工批准后发布为 canary，仅承担小比例任务。
6. **Promote/Rollback**：指标持续达标才晋级；否则自动回滚配置版本。

P0 只实现 Observe 和可复现的 Parser 测试基线。Propose/Replay/Gate 是下一阶段，避免在尚未证明需求时建设高风险自修改系统。
